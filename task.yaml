version: v1.1.0
stepTimeout: 3600
steps:
    - cmd: >-
          {{.Run.Registry}}/buildx
          build --push
          -t {{.Run.Registry}}/wknd:{{.Run.ID}}
          --cache-from={{.Run.Registry}}/wknd:cache
          --cache-to=type=registry,ref={{.Run.Registry}}/wknd:cache,mode=max
          https://github.com/akalfas/wknd.git
